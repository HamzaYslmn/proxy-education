[project]
name = "pwn-tools"
version = "0.1.0"
description = "Teaching CLI for Supabase misconfigurations — direct vs proxy"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.1",
    "httpx>=0.28.1",
]
